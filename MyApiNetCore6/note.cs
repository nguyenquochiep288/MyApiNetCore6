using System.Xml.Linq;

namespace API_QuanLyTHP
{
    public class note
    {
//        SELECT CASE
//    WHEN c.isnullable = 0 THEN
//    CONCAT('ALTER TABLE ', o.name, ' ALTER COLUMN ', c.name, ' nvarchar(', c.prec , ') COLLATE SQL_Latin1_General_CP1_CI_AS NOT NULL;')
//    ELSE CONCAT('ALTER TABLE ', o.name, ' ALTER COLUMN ', c.name, ' nvarchar(', c.prec , ') COLLATE SQL_Latin1_General_CP1_CI_AS NULL;')
//END
//FROM syscolumns c
//     INNER JOIN sysobjects o ON o.id = c.id
//     LEFT JOIN systypes t on t.xtype = c.xtype
//WHERE o.type = 'U' and t.name = 'nvarchar' and c.prec != -1 and c.name like 'ID_%'
    }
}
